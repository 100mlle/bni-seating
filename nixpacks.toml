[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --cache /tmp/npm-cache --no-fund --no-audit --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
